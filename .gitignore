# macOS
.DS_Store

# Claude Code session data
.claude/

# Source / backup assets (originals kept locally, don't deploy)
_archive/

# Dev-only pages (not linked from index.html)
admin.html
admin.css
admin.js
brief.html
fonts.html
fonts-showcase.html

# CB variant — old standalone fork (cb-script.js / cb-style.css) is deprecated.
# cb.html now mirrors custom.html with a forced company=California+Burrito param,
# so it's part of the main deploy. Keep the old asset filenames here so stale
# copies on disk don't accidentally get committed.
cb-script.js
cb-style.css

# Local dev server helper
server.py

# Side projects that live in this repo but aren't part of the site
jobSearch/
magesta/
v2/

# Unused asset folders
# (keep /cropped and /personal_photos — those are referenced)
